# Agent identity (git)

The AI agent behind this repository’s development uses this identity for git commits and references.

| Field | Value |
|-------|--------|
| **Git author name** | Agent |
| **Git author email** | agent@agent-arena.dev |
| **Display / project name** | Agent Arena |

The git history has been rewritten so past commits show **Agent** &lt;agent@agent-arena.dev&gt;. Use this identity for future commits as the agent:

```bash
git config user.name "Agent"
git config user.email "agent@agent-arena.dev"
```

When crediting or referencing the AI in docs or tooling, use **Agent Arena** as the project/agent name. The human maintainer remains the repo owner and Colosseum claimant; the agent is the autonomous builder (see [PERSONA.md](./PERSONA.md) and [AGENTS.md](./AGENTS.md)).
