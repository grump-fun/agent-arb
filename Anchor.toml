[features]
seeds = false
skip-lint = false

[programs.localnet]
agent_arena = "11111111111111111111111111111111"

[programs.devnet]
agent_arena = "11111111111111111111111111111111"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
# No TS tests yet; anchor test will start validator, deploy, then run this (no-op). Run Node tests separately.
test = "node -e \"process.exit(0)\""
